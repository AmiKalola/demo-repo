name: first demo 
on:
 push:
  branches: 
  - main 
jobs:
  demo_job:
   runs-on: self-hosted
   steps:
    - uses: actions/checkout@v3
    - name: run a one-line script
      run: | 
       rsync -zhvr ./* ubuntu@15.206.117.174:/home/ubuntu/test/
    #- name: run multiple-line script
      # run: | 
      #    echo Nothing to show here..
       #   echo what is in that folder?
        #  ls -a 
   
